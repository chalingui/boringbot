# Copy to .env (repo root) or config/.env
BYBIT_API_KEY=
BYBIT_API_SECRET=
BYBIT_BASE_URL=https://api.bybit.com
BYBIT_RECV_WINDOW=5000
BYBIT_ACCOUNT_TYPE=SPOT

# Symbols (Bybit Spot symbols)
SYMBOL_TRADE=ETHUSDT
SYMBOL_PROFIT_CONVERT=USDCUSDT

# Strategy
DCA_AMOUNT_USDT=100
DCA_INTERVAL_DAYS=7
SELL_MARKUP_PCT=5

# Email notifications (optional)
# IMPORTANT: do NOT commit real credentials; keep them only in your local .env
NOTIFY_ENABLED=0
NOTIFY_EMAIL_TO=
NOTIFY_EMAIL_FROM=
NOTIFY_COOLDOWN_MINUTES=720

SMTP_HOST=
SMTP_PORT=587
SMTP_ENCRYPTION=starttls
SMTP_USER=
SMTP_PASS=
